# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
.next/
.turbo/
.expo/
*.tsbuildinfo

# Environment
.env
.env.*
!.env.example
!deploy/.env.prod.example

# Session / tooling artifacts
.superpowers/
.DS_Store
coverage/
*.log

# Brainstorming visual companion artifacts
.superpowers/

# Native build artifacts (regenerated by `expo prebuild` / CocoaPods / Gradle)
apps/mobile/ios/Pods/
apps/mobile/ios/build/
apps/mobile/ios/DerivedData/
**/*.xcworkspace/xcuserdata/
**/*.xcodeproj/xcuserdata/
apps/mobile/android/.gradle/
apps/mobile/android/local.properties
*.keystore
!debug.keystore
*.hprof

# Playwright MCP scratch output
.playwright-mcp/

# Local DB backups (deploy/backup.sh)
/backups/
deploy/backups/

# Terraform (deploy/oci) — local state and account-specific vars
deploy/oci/.terraform/
deploy/oci/.terraform.lock.hcl
deploy/oci/*.tfstate
deploy/oci/*.tfstate.*
deploy/oci/*.tfvars
!deploy/oci/*.tfvars.example
deploy/oci/crash.log

# Terraform (deploy/azure) — local state and subscription-specific vars
deploy/azure/.terraform/
deploy/azure/.terraform.lock.hcl
deploy/azure/*.tfstate
deploy/azure/*.tfstate.*
deploy/azure/*.tfvars
!deploy/azure/*.tfvars.example
deploy/azure/crash.log
